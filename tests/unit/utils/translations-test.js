import translations from 'isp/utils/translations';
import { module, test } from 'qunit';

module('Unit | Utility | translations');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = translations();
  assert.ok(result);
});
