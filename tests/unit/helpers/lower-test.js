import { lower } from 'isp/helpers/lower';
import { module, test } from 'qunit';

module('Unit | Helper | lower');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = lower([42]);
  assert.ok(result);
});
