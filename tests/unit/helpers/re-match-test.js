import { reMatch } from 'isp/helpers/re-match';
import { module, test } from 'qunit';

module('Unit | Helper | re match');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = reMatch([42]);
  assert.ok(result);
});
