{{#bs-form}}
  {{#each-in questions as |question parts|}}
    {{#bs-form-group}}
      <label>{{parts.question}}</label><br>
      {{#if (eq parts.type 'radio')}}
        {{#each-in parts.items as |item data|}}
          {{#if data.description}}
            <p>{{data.description}}</p>
          {{/if}}
          {{radio-group options=parts.scale labelTop=data.labelTop labels=data.labels value=data.value}}
          {{#if showValidation}}
            <div class="text-danger">
              {{v-get this (concat 'questions.' question '.items.' item '.value') 'message'}}
            </div>
          {{/if}}
        {{/each-in}}
      {{else if (eq parts.type 'select')}}
        {{#if showValidation}}
          <span class="text-danger">
            {{v-get this (concat 'questions.' question '.items.item1.value') 'message'}}
          </span>
        {{/if}}
        <br>
        {{select-input options=parts.scale value=parts.items.item1.value}}
      {{else if (eq parts.type 'radio-input')}}
          {{radio-group options=parts.scale value=parts.items.radio.value}}
          {{#if showValidation}}
            <div class="text-danger">
              {{v-get this (concat 'questions.' question '.items.radio.value') 'message'}}
            </div>
          {{/if}}
          {{#if (eq parts.items.radio.value 'Yes')}}
            <p>{{parts.items.input.description}}</p>
            {{#input value=parts.input.value}}{{/input}}
            {{#if showValidation}}
              <div class="text-danger">
                {{v-get this (concat 'questions.' question '.items.input.value') 'message'}}
              </div>
            {{/if}}
            <br>
          {{/if}}
      {{else if (eq parts.type 'textarea')}}
        {{#textarea value=parts.input.value cols="100" rows="6"}}{{/textarea}}
          {{#if showValidation}}
            <div class="text-danger">
                {{v-get this (concat 'questions.' question '.items.input.value') 'message'}}
            </div>
          {{/if}}
      {{/if}}
    {{/bs-form-group}}
  {{/each-in}}
  {{bs-button class="pull-right" defaultText="Continue" type="primary" action='nextSection'}}
{{/bs-form}}
